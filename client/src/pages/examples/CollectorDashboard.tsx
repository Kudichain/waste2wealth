import { ThemeProvider } from "@/components/ThemeProvider";
import CollectorDashboard from "../CollectorDashboard";

export default function CollectorDashboardExample() {
  return (
    <ThemeProvider>
      <CollectorDashboard />
    </ThemeProvider>
  );
}
